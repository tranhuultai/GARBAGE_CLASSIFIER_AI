# Keras 3 chạy trên PyTorch (không dùng TensorFlow) - hoạt động trên mọi bản Python
# (khó hơn TensorFlow, thường chỉ hỗ trợ tới Python 3.12).
keras>=3.0
torch>=2.0
streamlit>=1.30
numpy
pillow
scikit-learn
matplotlib
kagglehub

# Dung de test va lint, khong can cho luc chay app that
pytest
pylint
